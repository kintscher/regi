{
  "name": "@regi/ingest",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@regi/db": "workspace:*",
    "zod": "^3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "@types/node": "^20",
    "typescript": "^5",
    "wrangler": "^4"
  }
}
